<%= form_with(model: score_board, local: true) do |form| %>
  <% if score_board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(score_board.errors.count, "error") %> prohibited this score_board from being saved:</h2>

      <ul>
        <% score_board.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :score_id %>
    <%= form.number_field :score_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
