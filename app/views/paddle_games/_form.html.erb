<%= form_with(model: paddle_game, local: true) do |form| %>
  <% if paddle_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(paddle_game.errors.count, "error") %> prohibited this paddle_game from being saved:</h2>

      <ul>
        <% paddle_game.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
